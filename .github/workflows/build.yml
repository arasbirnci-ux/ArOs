name: Android Build OS
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build
        run: |
          echo "Mimari altyapı hazırlandı. Android fabrikası kuruluyor..."
          # Buraya indirme ve derleme komutlarımızı ekleyeceğiz
          # Şimdilik sadece fabrikanın çalıştığını test ediyoruz
          date
